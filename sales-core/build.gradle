apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile project(":sales-externals")
    compile group: 'org.slf4j', name : 'slf4j-api', version: "${slf4jVersion}"
    testRuntime group: 'ch.qos.logback', name : 'logback-classic', version: "${logbackVersion}"
}
